<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <!-- Define any variables or imports if needed -->
    </data>

    <!-- Wrap everything inside a ScrollView -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true"> <!-- Ensures the content fills the viewport -->

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"
            android:background="@color/backgroundColor"
            android:gravity="center">

            <!-- App Logo -->
            <ImageView
                android:id="@+id/logo"
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:layout_gravity="center"
                android:src="@drawable/logo"
                android:contentDescription="@string/app_name" />

            <!-- App Title -->
            <TextView
                android:id="@+id/title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_name"
                android:textSize="28sp"
                android:textColor="@color/primaryColor"
                android:gravity="center"
                android:layout_gravity="center"
                android:paddingTop="15dp"
                android:paddingBottom="25dp"
                android:textAlignment="center"
                android:fontFamily="@font/roboto_bold"/>

            <!-- Buttons Layout -->
            <LinearLayout
                android:id="@+id/buttonLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:paddingTop="20dp"
                android:paddingBottom="20dp">
                <!-- Add your buttons programmatically or through layout -->
            </LinearLayout>

            <!-- Small Buttons Layout -->
            <LinearLayout
                android:id="@+id/smallButtonLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:paddingTop="20dp"
                android:paddingBottom="20dp"
                android:layout_marginTop="20dp"
                android:layout_marginBottom="20dp">
                <!-- Additional small buttons will be added programmatically or in layout -->
            </LinearLayout>
        </LinearLayout>
    </ScrollView>

</layout>
